@using PackerTracker.Models;

<h2>Package Tracker</h2>

@if (Model.Count == 0)
{
<p>No items to be packed</p>

}
<ul>
@foreach(Packer item in Model)
{
  <li>@item.Items</li>
}
</ul>

<a href="/packer/new">Add a new item</a>